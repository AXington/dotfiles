[ids]

*

# Remap Meta+letter shortcuts to Ctrl equivalents (macOS Cmd key muscle memory).
# Undefined Meta+key combinations pass through untouched, preserving KDE shortcuts
# (Meta alone, Meta+Space, Meta+Tab, Meta+D, etc.).
[meta]

c = C-c
v = C-v
x = C-x
z = C-z
a = C-a
